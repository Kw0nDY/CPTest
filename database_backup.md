# DXT Enterprise AI Fabric - 데이터베이스 백업 가이드

## 📊 데이터베이스 정보

**연결 정보:**
- **데이터베이스명:** neondb
- **호스트:** ep-bitter-frog-afe21mru.c-2.us-west-2.aws.neon.tech
- **포트:** 5432
- **사용자:** neondb_owner
- **비밀번호:** npg_0o2HTvrgDyiB
- **백업 날짜:** 2025-01-27
- **총 테이블 수:** 25개

**연결 URL:**
```
postgresql://neondb_owner:npg_0o2HTvrgDyiB@ep-bitter-frog-afe21mru.c-2.us-west-2.aws.neon.tech/neondb?sslmode=require
```

## 🗂️ 전체 테이블 스키마 생성 스크립트

```sql
-- =============================================================================
-- DXT Enterprise AI Fabric Database Schema
-- Generated: 2025-01-27
-- =============================================================================

-- 1. 사용자 관리
CREATE TABLE users (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    username TEXT UNIQUE NOT NULL,
    name TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- 2. AI 모델 폴더 관리
CREATE TABLE ai_model_folders (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT DEFAULT '',
    color TEXT DEFAULT '#3B82F6',
    icon TEXT DEFAULT 'FolderOpen',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 3. AI 모델 관리
CREATE TABLE ai_models (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    file_name TEXT NOT NULL,
    file_size INTEGER,
    model_type TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'uploading',
    file_path TEXT,
    analysis_status TEXT NOT NULL DEFAULT 'pending',
    input_specs JSON DEFAULT '[]',
    output_specs JSON DEFAULT '[]',
    metadata JSON,
    configuration JSON,
    uploaded_at TIMESTAMP DEFAULT NOW(),
    analyzed_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    config_file_path TEXT,
    folder_id TEXT
);

-- 4. AI 모델 파일 관리
CREATE TABLE ai_model_files (
    id TEXT PRIMARY KEY,
    model_id TEXT NOT NULL,
    file_name TEXT NOT NULL,
    original_file_name TEXT NOT NULL,
    file_path TEXT NOT NULL,
    file_type TEXT NOT NULL,
    file_size INTEGER,
    mime_type TEXT,
    uploaded_at TIMESTAMP DEFAULT NOW(),
    created_at TIMESTAMP DEFAULT NOW()
);

-- 5. AI 모델 실행 결과
CREATE TABLE ai_model_results (
    id TEXT PRIMARY KEY,
    configuration_id TEXT,
    configuration_name TEXT,
    model_id TEXT NOT NULL,
    execution_type TEXT NOT NULL,
    input_data JSON,
    results JSON NOT NULL,
    status TEXT NOT NULL DEFAULT 'completed',
    execution_time INTEGER,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 6. 데이터 소스 관리
CREATE TABLE data_sources (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    category TEXT NOT NULL,
    vendor TEXT,
    status TEXT NOT NULL DEFAULT 'disconnected',
    config JSON NOT NULL,
    connection_details JSON,
    credentials JSON,
    last_sync TIMESTAMP,
    record_count INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 7. 데이터 테이블 스키마
CREATE TABLE data_tables (
    id TEXT PRIMARY KEY,
    data_source_id TEXT NOT NULL,
    table_name TEXT NOT NULL,
    fields JSON NOT NULL,
    record_count INTEGER DEFAULT 0,
    last_updated TIMESTAMP DEFAULT NOW()
);

-- 8. 뷰/대시보드 관리
CREATE TABLE views (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT NOT NULL,
    type TEXT NOT NULL,
    status TEXT NOT NULL,
    assigned_to JSON DEFAULT '[]',
    assigned_departments JSON DEFAULT '[]',
    data_sources JSON DEFAULT '[]',
    layout JSON DEFAULT '{"grids":[]}',
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

-- 9. 모델 설정 관리
CREATE TABLE model_configurations (
    id TEXT PRIMARY KEY,
    model_id TEXT,
    name TEXT NOT NULL,
    description TEXT,
    is_active INTEGER NOT NULL DEFAULT 0,
    input_mappings JSON DEFAULT '[]',
    output_mappings JSON DEFAULT '[]',
    settings JSON DEFAULT '{}',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    folder_id TEXT,
    status TEXT NOT NULL DEFAULT 'draft',
    nodes JSON DEFAULT '[]',
    connections JSON DEFAULT '[]'
);

-- 10. 모델 설정 폴더
CREATE TABLE model_configuration_folders (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT DEFAULT '',
    color TEXT DEFAULT '#3B82F6',
    icon TEXT DEFAULT 'FolderOpen',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 11. Excel 파일 관리
CREATE TABLE excel_files (
    id TEXT PRIMARY KEY,
    data_source_id TEXT NOT NULL,
    file_name TEXT NOT NULL,
    file_size INTEGER,
    uploaded_at TIMESTAMP DEFAULT NOW(),
    status TEXT NOT NULL DEFAULT 'processing',
    sheets JSON DEFAULT '[]',
    metadata JSON
);

-- 12. Google API 설정
CREATE TABLE google_api_configs (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    type TEXT NOT NULL,
    client_id TEXT NOT NULL,
    client_secret TEXT NOT NULL,
    project_id TEXT,
    api_key TEXT,
    scopes JSON DEFAULT '[]',
    status TEXT NOT NULL DEFAULT 'active',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 13. 산업 데이터 - 제조업 관련
CREATE TABLE wafer_data (
    id TEXT PRIMARY KEY,
    wafer_id TEXT NOT NULL,
    lot_id TEXT NOT NULL,
    process_step TEXT NOT NULL,
    equipment_id TEXT NOT NULL,
    processed_at TIMESTAMP DEFAULT NOW(),
    last_update TIMESTAMP DEFAULT NOW()
);

CREATE TABLE process_parameters (
    id TEXT PRIMARY KEY,
    wafer_id TEXT NOT NULL,
    equipment_id TEXT NOT NULL,
    process_step TEXT NOT NULL,
    temperature INTEGER,
    pressure INTEGER,
    gas_flow INTEGER,
    rf_power INTEGER,
    process_time INTEGER,
    recorded_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE quality_inspection (
    id TEXT PRIMARY KEY,
    wafer_id TEXT NOT NULL,
    inspection_type TEXT NOT NULL,
    defect_count INTEGER,
    thickness_measurement INTEGER,
    critical_dimension INTEGER,
    yield INTEGER,
    inspected_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE equipment_status (
    id TEXT PRIMARY KEY,
    equipment_id TEXT NOT NULL,
    equipment_type TEXT NOT NULL,
    status TEXT NOT NULL,
    utilization_rate INTEGER,
    chamber_pressure INTEGER,
    chamber_temperature INTEGER,
    error_code TEXT,
    last_maintenance_date TIMESTAMP,
    status_updated_at TIMESTAMP DEFAULT NOW()
);

-- 14. PI 시스템 데이터
CREATE TABLE pi_asset_hierarchy (
    id TEXT PRIMARY KEY,
    asset_name TEXT NOT NULL,
    asset_path TEXT NOT NULL,
    asset_type TEXT,
    location TEXT,
    operational_status TEXT,
    last_update TIMESTAMP DEFAULT NOW()
);

CREATE TABLE pi_tag_values (
    id TEXT PRIMARY KEY,
    tag_name TEXT NOT NULL,
    asset_path TEXT,
    value INTEGER,
    quality TEXT NOT NULL DEFAULT 'Good',
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    scale_flag INTEGER DEFAULT 1000,
    recorded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE pi_event_frames (
    id TEXT PRIMARY KEY,
    event_name TEXT NOT NULL,
    template_name TEXT NOT NULL,
    asset_path TEXT NOT NULL,
    start_time TIMESTAMP NOT NULL,
    end_time TIMESTAMP,
    duration INTEGER,
    event_type TEXT,
    acknowledged INTEGER DEFAULT 0,
    severity TEXT DEFAULT 'Normal',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE pi_drilling_operations (
    id TEXT PRIMARY KEY,
    well_pad_id TEXT NOT NULL,
    bit_weight INTEGER,
    block_height INTEGER,
    diff_press INTEGER,
    flow_in_rate INTEGER,
    hole_depth INTEGER,
    hook_load INTEGER,
    pump_pressure INTEGER,
    top_drive_rpm INTEGER,
    top_drive_torque INTEGER,
    timestamp TIMESTAMP DEFAULT NOW()
);

CREATE TABLE pi_api_responses (
    id TEXT PRIMARY KEY,
    api_endpoint TEXT NOT NULL,
    request_type TEXT NOT NULL,
    response_format TEXT NOT NULL,
    sample_data JSONB,
    data_structure TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 15. 외부 시스템 연동
CREATE TABLE sap_customers (
    id TEXT PRIMARY KEY,
    customer_id TEXT NOT NULL,
    customer_name TEXT NOT NULL,
    country TEXT,
    credit_limit INTEGER,
    created_date TEXT,
    last_update TIMESTAMP DEFAULT NOW()
);

CREATE TABLE sap_orders (
    id TEXT PRIMARY KEY,
    order_id TEXT NOT NULL,
    customer_id TEXT NOT NULL,
    order_date TEXT,
    total_amount INTEGER,
    status TEXT,
    last_update TIMESTAMP DEFAULT NOW()
);

CREATE TABLE salesforce_accounts (
    id TEXT PRIMARY KEY,
    sf_id TEXT NOT NULL,
    name TEXT NOT NULL,
    industry TEXT,
    annual_revenue INTEGER,
    number_of_employees INTEGER,
    last_update TIMESTAMP DEFAULT NOW()
);

CREATE TABLE salesforce_opportunities (
    id TEXT PRIMARY KEY,
    sf_id TEXT NOT NULL,
    name TEXT NOT NULL,
    account_id TEXT,
    amount INTEGER,
    stage_name TEXT,
    close_date TEXT,
    last_update TIMESTAMP DEFAULT NOW()
);
```

## 📁 기본 데이터 삽입 스크립트

```sql
-- =============================================================================
-- 기본 데이터 삽입
-- =============================================================================

-- AI 모델 폴더 생성
INSERT INTO ai_model_folders (id, name, description, color, icon, created_at, updated_at) VALUES
('folder-1755752650792', 'User Model', '', '#3B82F6', 'FolderOpen', '2025-08-21 05:04:11.618899', '2025-08-21 05:04:11.618899'),
('folder-1755822061230', 'KPI Parameter', 'KPI and Parameter Problem Solve', '#10B981', 'FolderOpen', '2025-08-22 00:21:02.06509', '2025-08-22 00:21:02.06509');

-- 기본 뷰 생성
INSERT INTO views (id, name, description, type, status, assigned_to, assigned_departments, data_sources, layout, created_at, updated_at) VALUES
('view-1', 'Production Dashboard', 'Real-time production monitoring and KPIs', 'dashboard', 'paused', '["Operations Team"]', '["Manufacturing"]', '["ds-1755665670959"]', '{"grids":[]}', '2025-08-20', '2025-08-21'),
('view-2', 'Quality Monitor', 'Product quality metrics and defect tracking', 'monitor', 'active', '["Quality Team"]', '["Quality Control"]', '["ds-1755665673064"]', '{"grids":[]}', '2025-08-20', '2025-08-26');

-- 데이터 소스 설정
INSERT INTO data_sources (id, name, type, category, vendor, status, config, connection_details, record_count, created_at, updated_at) VALUES
('ds-1755665670959', 'SAP ERP System', 'SAP', 'Enterprise Resource Planning', 'SAP', 'connected', '{"server":"sap-prod.company.com","client":"100","systemNumber":"00"}', '{"host":"sap-prod.company.com","port":3200,"protocol":"RFC"}', 150000, '2025-08-20 04:54:31.078967', '2025-08-20 04:54:31.078967'),
('ds-1755665673064', 'Salesforce CRM', 'Salesforce', 'Customer Relationship Management', 'Salesforce', 'connected', '{"instance":"https://company.my.salesforce.com","apiVersion":"58.0"}', '{"instance":"https://company.my.salesforce.com","sandbox":false}', 45000, '2025-08-20 04:54:33.181862', '2025-08-20 04:54:33.181862'),
('ds-1755665675072', 'Oracle Database', 'Oracle', 'Database', 'Oracle', 'connected', '{"host":"oracle-prod.company.com","port":1521,"serviceName":"PROD"}', '{"host":"oracle-prod.company.com","port":1521}', 2500000, '2025-08-20 04:54:35.190728', '2025-08-20 04:54:35.190728'),
('ds-1755665677128', 'AVEVA PI System', 'AVEVA PI', 'Industrial Data', 'AVEVA', 'connected', '{"server":"pi-server.company.com","port":5450}', '{"server":"pi-server.company.com","port":5450}', 500000, '2025-08-20 04:54:37.245643', '2025-08-20 04:54:37.245643');
```

## 🔧 VS Code에서 사용하는 방법

### 1. PostgreSQL 익스텐션 설치
VS Code에서 `PostgreSQL` 확장을 설치합니다.

### 2. 데이터베이스 연결 설정
```json
{
  "name": "DXT Enterprise AI Fabric",
  "host": "ep-bitter-frog-afe21mru.c-2.us-west-2.aws.neon.tech",
  "port": 5432,
  "database": "neondb",
  "username": "neondb_owner",
  "password": "npg_0o2HTvrgDyiB",
  "ssl": true
}
```

### 3. 새 데이터베이스에 전체 스키마 복원
```sql
-- 위의 CREATE TABLE 스크립트들을 순서대로 실행
-- 그 다음 INSERT 스크립트들을 실행
```

### 4. 특정 테이블 확인 쿼리
```sql
-- 테이블 존재 확인
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- 특정 테이블 데이터 확인
SELECT * FROM ai_models LIMIT 10;
SELECT * FROM data_sources;
SELECT * FROM views;

-- 테이블별 레코드 수 확인
SELECT 
    table_name,
    (SELECT COUNT(*) FROM information_schema.columns WHERE table_name = t.table_name) as column_count
FROM information_schema.tables t
WHERE table_schema = 'public'
ORDER BY table_name;
```

## 📋 주요 테이블별 설명

### 🤖 AI 모델 관련 테이블
- **ai_models**: AI 모델 메타데이터 및 파일 정보
- **ai_model_folders**: 모델 폴더 구조 관리
- **ai_model_files**: 모델 파일 세부 정보
- **ai_model_results**: 모델 실행 결과
- **model_configurations**: 모델 설정 및 워크플로우

### 📊 데이터 통합 테이블
- **data_sources**: 외부 데이터 소스 연결 정보
- **data_tables**: 데이터 테이블 스키마 정보
- **views**: 사용자 정의 뷰 및 대시보드

### 🏭 산업 데이터 테이블
- **wafer_data**: 웨이퍼 제조 데이터
- **process_parameters**: 제조 공정 파라미터
- **quality_inspection**: 품질 검사 결과
- **equipment_status**: 장비 상태 모니터링

### 🔗 외부 시스템 연동
- **sap_customers/orders**: SAP 시스템 데이터
- **salesforce_accounts/opportunities**: Salesforce 데이터
- **pi_***: AVEVA PI 시스템 데이터

이 백업 가이드를 사용하여 언제든지 동일한 구조의 데이터베이스를 복원하거나 다른 환경에 배포할 수 있습니다.